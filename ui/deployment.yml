apiVersion: apps/v1
kind: Deployment
metadata:
  name: msa-sample-ui
  labels:
    app: msa-sample-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: msa-sample-ui
      tier: backend
  template:
    metadata:
      labels:
        app: msa-sample-ui
        tier: backend
    spec:
      containers:
        - name: msa-sample-ui
          image: ${IMAGE}:${BEFOREJOB_BUILD_NUMBER}
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: kpaas-msa-configmap
      imagePullSecrets:
        - name: ${SECRET_NAME}
